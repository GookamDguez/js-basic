/**
 * var: alcance global (para todo el codigo) y puede ser modificada.
const: alcance global, no se puede crear mas variables con el mismo nombre,solo se puede modificar su valor en caso de utilizar array utilizando push.
let: alcance local (solo se utiliza en el bloque que la declaramos como dentro de un for , while, fuera de este no afectará)
Es importantisimo para evitar errores en el futuro, que querramos llamar una variable y no exista porque solo esta local
 */

 //DECLARAR
var edad;
//INICIALIZAR
edad = 21;

var elementos = ["panes", "carne"];

var persona = {
    nombre: "soiyo",
    edad: 21
}

